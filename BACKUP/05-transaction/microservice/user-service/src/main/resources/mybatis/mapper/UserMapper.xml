<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springframework.boot.transaction.jta.user.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.example.springframework.boot.transaction.jta.user.entity.User">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
    </resultMap>

    <parameterMap id="user" type="com.example.springframework.boot.transaction.jta.user.entity.User"/>

    <insert id="insert" parameterMap="user" useGeneratedKeys="true" keyProperty="id">
        insert into `user` (`name`, `password`)
        values (#{name}, #{password})
    </insert>

    <delete id="clean">
        TRUNCATE TABLE `user`;
    </delete>

    <select id="queryAll" resultMap="BaseResultMap">
        select * from `user`
    </select>

</mapper>