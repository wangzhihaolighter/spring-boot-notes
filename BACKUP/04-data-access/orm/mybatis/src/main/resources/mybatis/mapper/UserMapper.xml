<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springframework.boot.mybatis.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.example.springframework.boot.mybatis.entity.User">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
    </resultMap>

    <resultMap id="BaseDetailResultMap" type="com.example.springframework.boot.mybatis.entity.User">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <association property="userInfo" resultMap="UserInfoResultMap"/>
        <collection property="userRoles" resultMap="UserRoleResultMap" javaType="java.util.List"/>
    </resultMap>

    <resultMap id="UserInfoResultMap" type="com.example.springframework.boot.mybatis.entity.UserInfo">
        <id column="ui_id" jdbcType="BIGINT" property="id"/>
        <result column="ui_user_id" jdbcType="BIGINT" property="userId"/>
        <result column="ui_country" jdbcType="VARCHAR" property="country"/>
        <result column="ui_province" jdbcType="VARCHAR" property="province"/>
    </resultMap>

    <resultMap id="UserRoleResultMap" type="com.example.springframework.boot.mybatis.entity.UserRole">
        <id column="ur_id" jdbcType="BIGINT" property="id"/>
        <result column="ur_user_id" jdbcType="BIGINT" property="userId"/>
        <result column="ur_role" jdbcType="VARCHAR" property="role"/>
    </resultMap>

    <parameterMap id="user" type="com.example.springframework.boot.mybatis.entity.User"/>

    <insert id="insert" parameterMap="user" useGeneratedKeys="true" keyProperty="id">
        insert into `user` (`username`, `password`)
        values (#{username}, #{password})
    </insert>

    <insert id="batchInsert" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="id">
        insert into `user` (`username`, `password`)
        values
        <foreach collection="list" item="user" separator="," index="index">
            (#{user.username}, #{user.password})
        </foreach>
    </insert>

    <update id="update" parameterType="com.example.springframework.boot.mybatis.entity.User">
        update `user` set
        <if test="username!=null and username!=''">
            username=#{username},
        </if>
        <if test="password!=null and password!=''">
            password=#{password},
        </if>
        id=id
        where id=#{id}
    </update>

    <update id="batchUpdate">
        update `user`
        <trim prefix="set" suffixOverrides=",">
            id=id,
            <trim prefix="username = case" suffix="end,">
                <foreach collection="list" index="index" item="user">
                    <if test="user.username != null and user.username != ''">
                        when id=#{user.id} then #{user.username}
                    </if>
                    <if test="user.password == null or user.password == ''">
                        when id=#{user.id} then username
                    </if>
                </foreach>
            </trim>
            <trim prefix="password = case" suffix="end,">
                <foreach collection="list" index="index" item="user">
                    <if test="user.password != null and user.password != ''">
                        when id=#{user.id} then #{user.password}
                    </if>
                    <if test="user.password == null or user.password == ''">
                        when id=#{user.id} then password
                    </if>
                </foreach>
            </trim>
        </trim>
        where id in
        <foreach collection="list" index="index" item="user" open="(" close=")" separator=",">
            #{user.id}
        </foreach>
    </update>

    <delete id="delete">
        delete from `user`
        where id=#{id}
    </delete>

    <delete id="batchDelete">
        delete from `user`
        where id in
        <foreach collection="list" item="id" index="index" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

    <select id="queryById" resultMap="BaseResultMap">
        select * from `user`
        where id=#{id}
    </select>

    <select id="queryByIds" resultMap="BaseResultMap">
        select * from `user`
        where id in
        <foreach collection="list" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </select>

    <select id="queryAll" resultMap="BaseResultMap">
        select * from `user`
    </select>

    <select id="queryUserDetailById" resultMap="BaseDetailResultMap">
        select
        u.*,
        ui.id as ui_id, ui.user_id as ui_user_id, ui.country as ui_country, ui.province as ui_province,
        ur.id as ur_id, ur.user_id as ur_user_id, ur.role as ur_role
        from `user` u
        left join `user_info` ui on u.id=ui.user_id
        left join `user_role` ur on u.id=ur.user_id
        where u.id=#{id}
    </select>

</mapper>